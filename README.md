# EE Operations Updates

This public repository is the update channel for EE Operations desktop users.

It contains only a signed release ZIP URL and its SHA-256 checksum in
`manifest.json`. It never contains OAuth credentials, Google Drive tokens,
WhatsApp sessions, user settings, or customer files.
